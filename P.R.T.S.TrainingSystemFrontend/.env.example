# ============================================================
# 前端环境变量模板 —— 可安全提交到版本库
# Frontend env template — safe to commit.
# 使用方式：复制为 .env 并填入真实值
#   cp .env.example .env
# 重要：只有以 VITE_ 开头的变量才会被 Vite 暴露给前端代码
#       （通过 import.meta.env.VITE_XXX 读取）。
# .env 已被本目录的 .gitignore 忽略，不会入库。
# ============================================================

# 后端 API 基础地址。
# 开发模式：Vite 已把 /api、/images 代理到 http://localhost:8080，
#           此处可保持默认值 /api/v1，无需改动。
# 生产构建：若前端与后端不同源，改为实际后端地址，例如 https://api.example.com
VITE_API_BASE_URL=/api/v1

# 说明：旧的 Vue2 多页应用（js/、*.html）不支持构建期变量注入，
# 其 API 地址通过运行时全局变量 window.API_BASE_URL 覆盖（见 js/config.js）。
